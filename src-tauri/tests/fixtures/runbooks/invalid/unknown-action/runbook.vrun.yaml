apiVersion: runbooks.veviad.com/v1alpha1
kind: Runbook
metadata:
  id: invalid-unknown-action
  version: 1.0.0
  title: Unknown action fixture
spec:
  target:
    kind: active-terminal
  steps:
    - id: inspect-host
      title: Inspect host
      check:
        uses: python
        with:
          module: platform
