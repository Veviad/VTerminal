## Windows shell startup with WSL

Creating a shell on Windows now preserves the terminal provided by WSL. This fixes startup failures that showed `setsid: failed to set the controlling terminal: Operation not permitted` and immediately exited even when WSL was installed and working.

Close the failed terminal tab and create a new shell after updating. Session cleanup still terminates processes started by the tab when you close it.
